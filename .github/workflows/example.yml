name: Example action
run-name: ${{ github.actor }} is learning github actions
on: push
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: pwd
        run: |
          cd ./ex-site
          ls
